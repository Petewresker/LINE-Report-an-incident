import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: 'export', /*สําหรับการแปลงไฟล์ tsx ให้กลายเป็น Vanilla (HTML CSS Js)*/
};

export default nextConfig;
